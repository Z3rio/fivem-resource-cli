import { UIFramework } from "../../utils/types";
import jqueryFiles from "../../files/ui/jquery"

const data: UIFramework = {
  label: "Jquery",
  files: [
    ...jqueryFiles
  ]
}

export default data
